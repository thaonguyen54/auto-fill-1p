import React from "react";
import RootContainer from "../../../utils/dom";

const Home = () => {
  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

RootContainer.getRoot().render(<Home />);

export default Home;
